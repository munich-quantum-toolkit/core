diff --git a/bindings/mlir/register_mlir.cpp b/bindings/mlir/register_mlir.cpp
index 1a961a4ad..d0a648bac 100644
--- a/bindings/mlir/register_mlir.cpp
+++ b/bindings/mlir/register_mlir.cpp
@@ -277 +277,3 @@ programFromInput(const nb::object& program, const bool inplace) {
-    return programFromString(nb::cast<std::string>(program));
+    const auto input = nb::cast<std::string>(program);
+    const nb::gil_scoped_release release;
+    return programFromString(input);
@@ -280 +282,3 @@ programFromInput(const nb::object& program, const bool inplace) {
-    return programFromPath(nb::cast<std::filesystem::path>(program));
+    const auto path = nb::cast<std::filesystem::path>(program);
+    const nb::gil_scoped_release release;
+    return programFromPath(path);
@@ -321,3 +325,4 @@ compileProgram(const nb::object& program, const mlir::ProgramFormat output,
-  return takeResult(mlir::runDefaultPipeline(programFromInput(program, inplace),
-                                             output, qcoPipeline, enableTiming,
-                                             enableStatistics));
+  auto input = programFromInput(program, inplace);
+  const nb::gil_scoped_release release;
+  return takeResult(mlir::runDefaultPipeline(
+      std::move(input), output, qcoPipeline, enableTiming, enableStatistics));
@@ -374,2 +379,6 @@ compileProgramForTarget(const nb::object& program, const nb::object& target,
-      return nb::cast(takeResult(mlir::runDefaultPipeline(
-          std::move(input), environment, enableTiming, enableStatistics)));
+      auto compiled = [&] {
+        const nb::gil_scoped_release release;
+        return takeResult(mlir::runDefaultPipeline(
+            std::move(input), environment, enableTiming, enableStatistics));
+      }();
+      return nb::cast(std::move(compiled));
@@ -377,2 +386,6 @@ compileProgramForTarget(const nb::object& program, const nb::object& target,
-    return nb::cast(takeResult(mlir::CompiledProgram::compile(
-        std::move(input), environment, enableTiming, enableStatistics)));
+    auto compiled = [&] {
+      const nb::gil_scoped_release release;
+      return takeResult(mlir::CompiledProgram::compile(
+          std::move(input), environment, enableTiming, enableStatistics));
+    }();
+    return nb::cast(std::move(compiled));
@@ -441,2 +454,2 @@ template <class Function>
-  auto compiled = takeResult(mlir::runDefaultPipeline(
-      programFromInput(program, false), mlir::ProgramFormat::QCO));
+  auto compiled = compileProgram(program, mlir::ProgramFormat::QCO, false,
+                                 "mqt-qco-default", false, false);
diff --git a/include/mqt-core/qdmi/Client.hpp b/include/mqt-core/qdmi/Client.hpp
index 4b35cbba1..754db9c3d 100644
--- a/include/mqt-core/qdmi/Client.hpp
+++ b/include/mqt-core/qdmi/Client.hpp
@@ -1020,6 +1020,10 @@ private:
-    return detail::queryProperty<T>(
-        [&](const size_t size, void* value, size_t* sizeRet) {
-          return QDMI_device_query_site_property(device_.get(), site_, prop,
-                                                 size, value, sizeRet);
-        },
-        msg, std::string("Querying size") + qdmi::toString(prop));
+    const auto query = [&](const size_t size, void* value, size_t* sizeRet) {
+      return QDMI_device_query_site_property(device_.get(), site_, prop, size,
+                                             value, sizeRet);
+    };
+    if constexpr (string_or_optional_string<T>) {
+      return detail::queryProperty<T>(
+          query, msg, std::string("Querying size") + qdmi::toString(prop));
+    } else {
+      return detail::queryProperty<T>(query, msg, msg);
+    }
diff --git a/test/python/test_mlir.py b/test/python/test_mlir.py
index 384445129..d76c2243d 100644
--- a/test/python/test_mlir.py
+++ b/test/python/test_mlir.py
@@ -14,0 +15,2 @@ import re
+import sys
+import time
@@ -15,0 +18 @@ from pathlib import Path
+from threading import Event, Thread
@@ -41,0 +45 @@ from mqt.core.mlir import (
+from mqt.core.qdmi import ProgramFormat
@@ -999,0 +1004,53 @@ def test_qc_program_num_gates() -> None:
+
+
+@pytest.mark.parametrize("mode", ["targetless", "target_output", "target_payload", "source", "path"])
+def test_native_compilation_releases_gil(tmp_path: Path, mode: str) -> None:
+    """Python threads progress during native parsing and each compilation overload."""
+    source = 'OPENQASM 3.0; include "stdgates.inc"; qubit[2] q;\n' + (
+        "rx(0.1) q[0]; cx q[0],q[1]; rz(0.2) q[1];\n" * 2000
+    )
+    target = CompilerTarget(
+        2,
+        connectivity=CompilerTarget.Connectivity.all_to_all(),
+        native_operations=CompilerTarget.NativeOperations.unrestricted(),
+    )
+    program = compile_program(source, output=OutputFormat.QCO)
+    invalid_source = source + "unknown_gate q[0];"
+    path = tmp_path / "invalid.qasm"
+    path.write_text(invalid_source, encoding="utf-8")
+    ready = Event()
+    stop = Event()
+    progress = 0
+
+    def worker() -> None:
+        nonlocal progress
+        ready.set()
+        while not stop.is_set():
+            progress += 1
+            time.sleep(0.001)
+
+    thread = Thread(target=worker)
+    interval = sys.getswitchinterval()
+    try:
+        # Prevent interpreter time slices around the native call from passing the check.
+        sys.setswitchinterval(60)
+        thread.start()
+        assert ready.wait(timeout=5)
+        before = progress
+        if mode == "targetless":
+            compile_program(program, output=OutputFormat.OPENQASM3)
+        elif mode == "target_output":
+            compile_program(program, target=target, output=OutputFormat.OPENQASM3)
+        elif mode == "target_payload":
+            compile_program(program, target=target, program_format=ProgramFormat.QASM3)
+        else:
+            # Parsing fails before the compilation release scope can be reached.
+            with pytest.raises(RuntimeError, match="MLIR operation failed"):
+                compile_program(path if mode == "path" else invalid_source, output=OutputFormat.QCO)
+        assert progress > before, "native parsing or compilation held the GIL"
+    finally:
+        stop.set()
+        thread.join(timeout=5)
+        sys.setswitchinterval(interval)
+    assert not thread.is_alive()
+    assert program.is_valid
