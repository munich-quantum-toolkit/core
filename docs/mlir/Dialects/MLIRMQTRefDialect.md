<!-- Autogenerated by mlir-tblgen; don't manually edit -->

# 'mqtref' Dialect

The MQT reference semantics (mqtref) dialect.

This dialect uses memory semantics to represent and work with qubits.
Similar to many existing quantum programming languages, operations can
just be applied to qubits without needing to track state updates.

This dialect is designed as a 'compatibility' dialect that simplifies
translations from and to existing languages such as QASM or QIR.
Its optimization capabilities are more limited compared to the
`mqtopt` dialect, which is designed for optimization.

The `mqtref` dialect supports dynamic as well as static qubit
addressing.

[TOC]

## Operations

[source](https://github.com/llvm/llvm-project/blob/main/mlir/include/mlir/Dialect/MQTRef/IR/MQTRefOps.td)

### `mqtref.allocQubit` (::mqt::ir::ref::AllocQubitOp)

_Allocates a single qubit_

Syntax:

```
operation ::= `mqtref.allocQubit` attr-dict
```

Allocates a single qubit in the |0> state.
The qubit can be used in operations after allocation.
It must be deallocated with the `deallocQubit` operation.

### Example (custom syntax)

```mlir
%q = mqtref.allocQubit
```

### Example (generic syntax)

```mlir
%q = "mqtref.allocQubit"() : () -> !mqtref.Qubit
```

Both are equivalent; the first form is enabled by this op's `assemblyFormat`.

Interfaces: `InferTypeOpInterface`

#### Results:

| Result  | Description                            |
| :-----: | -------------------------------------- |
| `qubit` | A (dynamic or static) qubit reference. |

### `mqtref.allocQubitRegister` (::mqt::ir::ref::AllocOp)

_Allocates a qubit register_

This class represents an operation that allocates a qubit register. The
size can either be specified during runtime as an operand or as an
attribute when the size is known at compile time. Before the qubits in
the register can be used, they need to be extracted from the register
with the extract operation.

Example:

```mlir
%0 = "mqtref.allocQubitRegister"() <{size_attr = 2 : i64}> : () -> !mqtref.QubitRegister

%c0_i64 = arith.constant 2 : i64
%1 = "mqtref.allocQubitRegister"(%c0_i64) : (i64) -> !mqtref.QubitRegister
```

Traits: `UniqueSizeDefinition`

Interfaces: `InferTypeOpInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>size_attr</code></td><td>::mlir::IntegerAttr</td><td>64-bit signless integer attribute whose value is non-negative</td></tr>
</table>

#### Operands:

| Operand | Description             |
| :-----: | ----------------------- |
| `size`  | 64-bit signless integer |

#### Results:

| Result | Description                   |
| :----: | ----------------------------- |
| `qreg` | An array of qubit references. |

### `mqtref.barrier` (::mqt::ir::ref::BarrierOp)

_Barrier operation_

Syntax:

```
operation ::= `mqtref.barrier` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents the barrier operation. It has a variadic number
of input qubits and no control qubits.

Traits: `AttrSizedOperandSegments`, `NoControl`, `NoParameter`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.dcx` (::mqt::ir::ref::DCXOp)

_DCX operation_

Syntax:

```
operation ::= `mqtref.dcx` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a DCX gate. It takes two qubits and a variadic
list of positive/negative controls as input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.deallocQubit` (::mqt::ir::ref::DeallocQubitOp)

_Deallocates a single qubit_

Syntax:

```
operation ::= `mqtref.deallocQubit` $qubit attr-dict
```

Deallocates a single qubit that was previously allocated with the
`allocQubit` operation. After this operation, the qubit is no longer
valid and cannot be used in further operations.

### Example (custom syntax)

```mlir
mqtref.deallocQubit %q
```

### Example (generic syntax)

```mlir
"mqtref.deallocQubit"(%0) : (!mqtref.Qubit) -> ()
```

Both are equivalent; the first form is enabled by this op's `assemblyFormat`.

#### Operands:

| Operand | Description                            |
| :-----: | -------------------------------------- |
| `qubit` | A (dynamic or static) qubit reference. |

### `mqtref.deallocQubitRegister` (::mqt::ir::ref::DeallocOp)

_Deallocates a qubit register_

This class represents an operation that deallocates a qubit register.
The qubit register is no longer valid after this operation.

Example:

```mlir
"mqtref.deallocQubitRegister"(%0) : (!mqtref.QubitRegister) -> ()
```

#### Operands:

| Operand | Description                   |
| :-----: | ----------------------------- |
| `qreg`  | An array of qubit references. |

### `mqtref.ecr` (::mqt::ir::ref::ECROp)

_DCX operation_

Syntax:

```
operation ::= `mqtref.ecr` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an ECR gate. It takes two qubits and a variadic
list of positive/negative controls as input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.extractQubit` (::mqt::ir::ref::ExtractOp)

_Extract a specific qubit reference from a given register_

This class represents an operation that extracts a specific qubit
reference from a given register. Before, the register must be
allocated with the allocateQubitRegister operation. Upon invocation
of this operation, the returned qubit reference can be used in
operations.

Example:

```mlir
%out_qubit = "mqtref.extractQubit"(%0) <{index_attr = 0 : i64}> : (!mqtref.QubitRegister) -> !mqtref.Qubit
```

Traits: `UniqueIndexDefinition`

Interfaces: `InferTypeOpInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>index_attr</code></td><td>::mlir::IntegerAttr</td><td>64-bit signless integer attribute whose value is non-negative</td></tr>
</table>

#### Operands:

|  Operand  | Description                   |
| :-------: | ----------------------------- |
| `in_qreg` | An array of qubit references. |
|  `index`  | 64-bit signless integer       |

#### Results:

|   Result    | Description                            |
| :---------: | -------------------------------------- |
| `out_qubit` | A (dynamic or static) qubit reference. |

### `mqtref.gphase` (::mqt::ir::ref::GPhaseOp)

_GPhase operation_

Syntax:

```
operation ::= `mqtref.gphase` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a global phase gate. It accepts a parameter indicating
the degree of the rotation angle. The global phase gate does not have
any input and output qubits. It might still be controlled by arbitrarily
many qubits.

Traits: `AttrSizedOperandSegments`, `NoTarget`, `OneParameter`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.h` (::mqt::ir::ref::HOp)

_H operation_

Syntax:

```
operation ::= `mqtref.h` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a Hadamard gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.i` (::mqt::ir::ref::IOp)

_I operation_

Syntax:

```
operation ::= `mqtref.i` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an identity gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.iswap` (::mqt::ir::ref::iSWAPOp)

_ISWAP operation_

Syntax:

```
operation ::= `mqtref.iswap` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an iSWAP gate. It takes two qubits and a variadic
list of positive/negative controls as input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.iswapdg` (::mqt::ir::ref::iSWAPdgOp)

_ISWAPdg operation_

Syntax:

```
operation ::= `mqtref.iswapdg` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an inverse iSWAP gate. It takes two qubits and a
variadic list of positive/negative controls as input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.measure` (::mqt::ir::ref::MeasureOp)

_A measure operation_

Syntax:

```
operation ::= `mqtref.measure` attr-dict $in_qubit
```

This class represents a measure operation. It takes a single qubit as
input and returns a bit. After the measurement, the referenced qubit
is either in the state |0> or |1> and is indicated by the returned bit.

Example:

```mlir
%1 = mqtref.measure %0
```

Interfaces: `InferTypeOpInterface`

#### Operands:

|  Operand   | Description                            |
| :--------: | -------------------------------------- |
| `in_qubit` | A (dynamic or static) qubit reference. |

#### Results:

|  Result   | Description            |
| :-------: | ---------------------- |
| `out_bit` | 1-bit signless integer |

### `mqtref.p` (::mqt::ir::ref::POp)

_P operation_

Syntax:

```
operation ::= `mqtref.p` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a phase gate. It takes a qubit and a variadic
list of positive/negative controls as an input. Additionally, it accepts
a parameter indicating the degree of the rotation angle.

Traits: `AttrSizedOperandSegments`, `OneParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.peres` (::mqt::ir::ref::PeresOp)

_Peres operation_

Syntax:

```
operation ::= `mqtref.peres` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a Peres gate. It takes two qubits and a variadic
list of positive/negative controls as input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.peresdg` (::mqt::ir::ref::PeresdgOp)

_Peresdg operation_

Syntax:

```
operation ::= `mqtref.peresdg` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an inverse Peres gate. It takes two qubits and a
variadic list of positive/negative controls as input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.qubit` (::mqt::ir::ref::QubitOp)

_Assign static qubit reference_

Syntax:

```
operation ::= `mqtref.qubit` attr-dict $index
```

The `mqtref.qubit` operation produces an SSA value from the given index
to a static (hardware) qubit.

Example:

```mlir
// Static (hardware) qubit with address '0'.
%q = "mqtref.qubit"() <{index = 0 : i64}> : () -> !mqtref.Qubit

// Using custom assembly.
%q = mqtref.qubit 0
```

Interfaces: `InferTypeOpInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>index</code></td><td>::mlir::IntegerAttr</td><td>64-bit signless integer attribute whose value is non-negative</td></tr>
</table>

#### Results:

| Result  | Description                            |
| :-----: | -------------------------------------- |
| `qubit` | A (dynamic or static) qubit reference. |

### `mqtref.reset` (::mqt::ir::ref::ResetOp)

_A reset operation_

This class represents a reset operation. It takes a single qubit as
input and and resets its value |0>.

Example:

```mlir
"mqtref.reset"(%0) : (!mqtopt.Qubit) -> ()
```

#### Operands:

|  Operand   | Description                            |
| :--------: | -------------------------------------- |
| `in_qubit` | A (dynamic or static) qubit reference. |

### `mqtref.rx` (::mqt::ir::ref::RXOp)

_RX operation_

Syntax:

```
operation ::= `mqtref.rx` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an RX gate. It takes a qubit and a variadic
list of positive/negative controls as input. Additionally, it accepts
a parameter indicating the degree of the rotation angle.

Traits: `AttrSizedOperandSegments`, `OneParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.rxx` (::mqt::ir::ref::RXXOp)

_RXX operation_

Syntax:

```
operation ::= `mqtref.rxx` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an RXX gate. It takes two qubits and a variadic
list of positive/negative controls as input. Additionally, it accepts a
parameter indicating the degree of the rotation angle.

Traits: `AttrSizedOperandSegments`, `OneParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.ry` (::mqt::ir::ref::RYOp)

_RY operation_

Syntax:

```
operation ::= `mqtref.ry` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an RY gate. It takes a qubit and a variadic
list of positive/negative controls as input. Additionally, it accepts
a parameter indicating the degree of the rotation angle.

Traits: `AttrSizedOperandSegments`, `OneParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.ryy` (::mqt::ir::ref::RYYOp)

_RYY operation_

Syntax:

```
operation ::= `mqtref.ryy` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an RYY gate. It takes two qubits and a variadic
list of positive/negative controls as input. Additionally, it accepts a
parameter indicating the degree of the rotation angle.

Traits: `AttrSizedOperandSegments`, `OneParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.rz` (::mqt::ir::ref::RZOp)

_RZ operation_

Syntax:

```
operation ::= `mqtref.rz` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an RZ gate. It takes a qubit and a variadic
list of positive/negative controls as input. Additionally, it accepts
a parameter indicating the degree of the rotation angle.

Traits: `AttrSizedOperandSegments`, `OneParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.rzx` (::mqt::ir::ref::RZXOp)

_RZX operation_

Syntax:

```
operation ::= `mqtref.rzx` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an RZX gate. It takes two qubits and a variadic
list of positive/negative controls as input. Additionally, it accepts a
parameter indicating the degree of the rotation angle.

Traits: `AttrSizedOperandSegments`, `OneParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.rzz` (::mqt::ir::ref::RZZOp)

_RZZ operation_

Syntax:

```
operation ::= `mqtref.rzz` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an RZZ gate. It takes two qubits and a variadic
list of positive/negative controls as input. Additionally, it accepts a
parameter indicating the degree of the rotation angle.

Traits: `AttrSizedOperandSegments`, `OneParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.s` (::mqt::ir::ref::SOp)

_S operation_

Syntax:

```
operation ::= `mqtref.s` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an S gate. It takes a qubit as an input and a
variadic list of positive/negative controls.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.sdg` (::mqt::ir::ref::SdgOp)

_Sdg operation_

Syntax:

```
operation ::= `mqtref.sdg` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an inverse S gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.swap` (::mqt::ir::ref::SWAPOp)

_SWAP operation_

Syntax:

```
operation ::= `mqtref.swap` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a SWAP gate. It takes two qubits and a variadic
list of positive/negative controls as input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.sx` (::mqt::ir::ref::SXOp)

_SX operation_

Syntax:

```
operation ::= `mqtref.sx` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an SX gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.sxdg` (::mqt::ir::ref::SXdgOp)

_SXdg operation_

Syntax:

```
operation ::= `mqtref.sxdg` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an inverse SX gate. It takes a qubit and a
variadic list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.t` (::mqt::ir::ref::TOp)

_T operation_

Syntax:

```
operation ::= `mqtref.t` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a T gate. It takes a qubit and a variadic list of
positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.tdg` (::mqt::ir::ref::TdgOp)

_Tdg operation_

Syntax:

```
operation ::= `mqtref.tdg` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an inverse T gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.u` (::mqt::ir::ref::UOp)

_U operation_

Syntax:

```
operation ::= `mqtref.u` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a U gate. It takes a qubit and a variadic
list of positive/negative controls as an input. Additionally, it accepts
three parameters indicating the degree of the rotation angles.

Traits: `AttrSizedOperandSegments`, `OneTarget`, `ThreeParameters`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.u2` (::mqt::ir::ref::U2Op)

_U2 operation_

Syntax:

```
operation ::= `mqtref.u2` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a U2 gate. It takes a qubit and a variadic
list of positive/negative controls as an input. Additionally, it accepts
two parameters indicating the degree of the rotation angles.

Traits: `AttrSizedOperandSegments`, `OneTarget`, `TwoParameters`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.v` (::mqt::ir::ref::VOp)

_V operation_

Syntax:

```
operation ::= `mqtref.v` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a V gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.vdg` (::mqt::ir::ref::VdgOp)

_Vdg operation_

Syntax:

```
operation ::= `mqtref.vdg` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an inverse V gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.x` (::mqt::ir::ref::XOp)

_X operation_

Syntax:

```
operation ::= `mqtref.x` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a Pauli-X gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.xx_minus_yy` (::mqt::ir::ref::XXminusYYOp)

_XX-YY operation_

Syntax:

```
operation ::= `mqtref.xx_minus_yy` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an XX-YY gate. It takes two qubits and a
variadic list of positive/negative controls as input. Additionally, it
accepts two parameters indicating the degree of the rotation angles.

Traits: `AttrSizedOperandSegments`, `TwoParameters`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.xx_plus_yy` (::mqt::ir::ref::XXplusYYOp)

_XX+YY operation_

Syntax:

```
operation ::= `mqtref.xx_plus_yy` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents an XX+YY gate. It takes two qubits and a
variadic list of positive/negative controls as input. Additionally, it
accepts two parameters indicating the degree of the rotation angles.

Traits: `AttrSizedOperandSegments`, `TwoParameters`, `TwoTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.y` (::mqt::ir::ref::YOp)

_Y operation_

Syntax:

```
operation ::= `mqtref.y` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a Pauli-Y gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

### `mqtref.z` (::mqt::ir::ref::ZOp)

_Z operation_

Syntax:

```
operation ::= `mqtref.z` `(` $params ( `static` $static_params^ )? ( `mask` $params_mask^ )? `)` attr-dict $in_qubits ( `ctrl` $pos_ctrl_in_qubits^ )? ( `nctrl` $neg_ctrl_in_qubits^ )?
```

This class represents a Pauli-Z gate. It takes a qubit and a variadic
list of positive/negative controls as an input.

Traits: `AttrSizedOperandSegments`, `NoParameter`, `OneTarget`

Interfaces: `UnitaryInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>static_params</code></td><td>::mlir::DenseF64ArrayAttr</td><td>f64 dense array attribute</td></tr>
<tr><td><code>params_mask</code></td><td>::mlir::DenseBoolArrayAttr</td><td>i1 dense array attribute</td></tr>
</table>

#### Operands:

|       Operand        | Description                                        |
| :------------------: | -------------------------------------------------- |
|       `params`       | variadic of 64-bit float                           |
|     `in_qubits`      | variadic of A (dynamic or static) qubit reference. |
| `pos_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |
| `neg_ctrl_in_qubits` | variadic of A (dynamic or static) qubit reference. |

## Types

### QubitRegisterType

An array of qubit references.

Syntax: `!mqtref.QubitRegister`

### QubitType

A (dynamic or static) qubit reference.

Syntax: `!mqtref.Qubit`
